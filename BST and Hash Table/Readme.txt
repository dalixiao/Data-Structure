Dali Xiao

BST.h is the class for Binary search tree

HashTable.h contains the class for hashing table

FolderTraversal.h is the method to traverse the parent directory and merge all the txt file into one large file: dataset.txt

quicksort.h is the sorting method for HashTable::sort

Work Flow inside main:

1 contructing two BST and HashTable using the string data in either hotels or hotels-small

2 Keep prompting user for input until user signals a kil
     Case 1.search
	  2.insert
	  3.delete
	  4.Sort the data structure, User should type in a output path
	  5.Range query. User should type in start word and end word separated by new line.


Project description is inside the folder

